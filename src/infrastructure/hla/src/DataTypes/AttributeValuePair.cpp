#include <functional>
#include <map>
#include <iomanip>
#include "DataTypes/AttributeValuePair.h"
#include <SDLDevStudio/Encoders.h>
#include <SDLDevStudio/Decoders.h>
#include <utils/Utils.h>

using namespace DevStudio;
using namespace std;

namespace SDL_RTI
{
	namespace DataTypes
	{
		constexpr uint32_t hash(const char* str, uint32_t h = 0) {
			return !str[h] ? 5381 : (hash(str, h + 1) * 33) ^ str[h];
		}

		struct AttributeMeta {
			std::function<void(void*&, const std::vector<char>&)> decodeFunc;
			std::function<std::vector<char>(void*)> encodeFunc;
		};
		AttributeValuePair::AttributeValuePair(HlaObjectInstanceBase::ObjectClassType objectType_, int attribute_, void* data_) : objectType(objectType_), attribute(attribute_), data(data_)
		{
            //if (!data)
            //    throw std::runtime_error("Null pointer received in encoder lambda for attribute: " + attributeName);
			attributeName = Utils::GetInstance().getAttributeName(objectType, attribute);
		}

		AttributeValuePair::AttributeValuePair(HlaObjectInstanceBase::ObjectClassType objectType_, std::string attributeName, void* data_) : objectType(objectType_), attributeName(attributeName), data(data_)
		{
            //if (!data)
            //    throw std::runtime_error("Null pointer received in encoder lambda for attribute: " + attributeName);
			attribute = Utils::GetInstance().getAttributeFromName(objectType, attributeName);
		}

		AttributeValuePair::AttributeValuePair(HlaObjectInstanceBase::ObjectClassType objectType_, const AttributeValuePairStruct& pairStruct) : objectType(objectType_), data(nullptr)
		{
            //if (!data)
            //    throw std::runtime_error("Null pointer received in encoder lambda for attribute: " + attributeName);
			attributeName = Utils::GetInstance().getAttributeName(objectType, pairStruct.attributeHandle);
			decode(pairStruct);
		}

		std::vector<AttributeValuePair> AttributeValuePair::getVector(const HlaObjectInstanceBase::ObjectClassType& classType, const std::vector<AttributeValuePairStruct>& attributeValueSet)
		{
			std::vector<AttributeValuePair> ret;
			ret.reserve(attributeValueSet.size());
			for (const auto& pairStruct : attributeValueSet)
			{
				ret.push_back(AttributeValuePair(classType, pairStruct));
			}
			return ret;
		}

		static const std::map<std::string, AttributeMeta> attributeMetaTable = {
//pragma region Autogenerated code
			// === AUTO-GENERATED BEGIN ===
        {"ACOUSTICS_IDENTIFIER", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodechar(bytes);
                data = new char(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodechar(*static_cast<char*>(data));
            }
        }},
        {"ACOUSTIC_NAME", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeActiveSonarEnum(bytes);
                data = new DevStudio::ActiveSonarEnum::ActiveSonarEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeActiveSonarEnum(*static_cast<DevStudio::ActiveSonarEnum::ActiveSonarEnum*>(data));
            }
        }},
        {"ACOUSTIC_SIGNATURE_INDEX", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeshort(bytes);
                data = new short(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeshort(*static_cast<short*>(data));
            }
        }},
        {"ACTIVE_EMISSION_PARAMETER_INDEX", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeshort(bytes);
                data = new short(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeshort(*static_cast<short*>(data));
            }
        }},
        {"ACTIVE_SONAR_IDENTIFIER", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodestring(bytes);
                data = new std::string(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodestring(*static_cast<std::string*>(data));
            }
        }},
        {"ACTIVE_STATUS", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeMinefieldStatusEnum(bytes);
                data = new DevStudio::MinefieldStatusEnum::MinefieldStatusEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeMinefieldStatusEnum(*static_cast<DevStudio::MinefieldStatusEnum::MinefieldStatusEnum*>(data));
            }
        }},
        {"ACTIVITY_PARAMETER", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeshort(bytes);
                data = new short(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeshort(*static_cast<short*>(data));
            }
        }},
        {"AFTERBURNER_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"AGENT_CLASS", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeAgentClassEnum(bytes);
                data = new DevStudio::AgentClassEnum::AgentClassEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeAgentClassEnum(*static_cast<DevStudio::AgentClassEnum::AgentClassEnum*>(data));
            }
        }},
        {"AGENT_TYPE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeAgentTypeEnum(bytes);
                data = new DevStudio::AgentTypeEnum::AgentTypeEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeAgentTypeEnum(*static_cast<DevStudio::AgentTypeEnum::AgentTypeEnum*>(data));
            }
        }},
        {"AGGREGATE_MARKING", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeAggregateMarkingStruct(bytes);
                data = new DevStudio::AggregateMarkingStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeAggregateMarkingStruct(*static_cast<DevStudio::AggregateMarkingStruct*>(data));
            }
        }},
        {"AGGREGATE_STATE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeAggregateStateEnum(bytes);
                data = new DevStudio::AggregateStateEnum::AggregateStateEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeAggregateStateEnum(*static_cast<DevStudio::AggregateStateEnum::AggregateStateEnum*>(data));
            }
        }},
        {"ALARM", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeCBRNAlarmStruct(bytes);
                data = new DevStudio::CBRNAlarmStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeCBRNAlarmStruct(*static_cast<DevStudio::CBRNAlarmStruct*>(data));
            }
        }},
        {"ALTERNATE_ENTITY_TYPE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeEntityTypeStruct(bytes);
                data = new DevStudio::EntityTypeStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeEntityTypeStruct(*static_cast<DevStudio::EntityTypeStruct*>(data));
            }
        }},
        {"ALTERNATE_MODE4", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeIffAlternateMode4Enum(bytes);
                data = new DevStudio::IffAlternateMode4Enum::IffAlternateMode4Enum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeIffAlternateMode4Enum(*static_cast<DevStudio::IffAlternateMode4Enum::IffAlternateMode4Enum*>(data));
            }
        }},
        {"ANCHOR_LIGHTS_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"ANTENNA_RAISED", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"ANTI_COLLISION_LIGHTS_DAY_NIGHT", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeAppearanceAntiCollisionDayNightEnum(bytes);
                data = new DevStudio::AppearanceAntiCollisionDayNightEnum::AppearanceAntiCollisionDayNightEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeAppearanceAntiCollisionDayNightEnum(*static_cast<DevStudio::AppearanceAntiCollisionDayNightEnum::AppearanceAntiCollisionDayNightEnum*>(data));
            }
        }},
        {"ANTI_COLLISION_LIGHTS_FLASHING", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeAntiCollisionLightFlashingEnum(bytes);
                data = new DevStudio::AntiCollisionLightFlashingEnum::AntiCollisionLightFlashingEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeAntiCollisionLightFlashingEnum(*static_cast<DevStudio::AntiCollisionLightFlashingEnum::AntiCollisionLightFlashingEnum*>(data));
            }
        }},
        {"ANTI_COLLISION_LIGHTS_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"AVERAGING_TIME", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeint(bytes);
                data = new int(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeint(*static_cast<int*>(data));
            }
        }},
        {"AZIMUTH_BEAMWIDTH", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"AZIMUTH_CENTER", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"BAROMETRIC_PRESSURE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"BEAM_AZIMUTH_CENTER", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"BEAM_AZIMUTH_SWEEP", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"BEAM_ELEVATION_CENTER", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"BEAM_ELEVATION_SWEEP", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"BEAM_FUNCTION_CODE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeBeamFunctionCodeEnum(bytes);
                data = new DevStudio::BeamFunctionCodeEnum::BeamFunctionCodeEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeBeamFunctionCodeEnum(*static_cast<DevStudio::BeamFunctionCodeEnum::BeamFunctionCodeEnum*>(data));
            }
        }},
        {"BEAM_IDENTIFIER", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodechar(bytes);
                data = new char(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodechar(*static_cast<char*>(data));
            }
        }},
        {"BEAM_SWEEP_SYNC", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"BLACKOUT_LIGHTS_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"BLACK_OUT_BRAKE_LIGHTS_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"BLACK_OUT_LIGHTS_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"BOTTOM_TYPE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeSedimentTypeEnum(bytes);
                data = new DevStudio::SedimentTypeEnum::SedimentTypeEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeSedimentTypeEnum(*static_cast<DevStudio::SedimentTypeEnum::SedimentTypeEnum*>(data));
            }
        }},
        {"BRAKE_LIGHTS_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"BREACHED_STATUS", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeBreachedStatusEnum(bytes);
                data = new DevStudio::BreachedStatusEnum::BreachedStatusEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeBreachedStatusEnum(*static_cast<DevStudio::BreachedStatusEnum::BreachedStatusEnum*>(data));
            }
        }},
        {"CALLSIGN", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodewstring(bytes);
                data = new std::wstring(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodewstring(*static_cast<std::wstring*>(data));
            }
        }},
        {"CAMOUFLAGE_TYPE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeCamouflageEnum(bytes);
                data = new DevStudio::CamouflageEnum::CamouflageEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeCamouflageEnum(*static_cast<DevStudio::CamouflageEnum::CamouflageEnum*>(data));
            }
        }},
        {"CAPTURE_STATUS", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeCaptureStatusEnum(bytes);
                data = new DevStudio::CaptureStatusEnum::CaptureStatusEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeCaptureStatusEnum(*static_cast<DevStudio::CaptureStatusEnum::CaptureStatusEnum*>(data));
            }
        }},
        {"CHEMICAL_CONTENT", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeChemicalContentEnum(bytes);
                data = new DevStudio::ChemicalContentEnum::ChemicalContentEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeChemicalContentEnum(*static_cast<DevStudio::ChemicalContentEnum::ChemicalContentEnum*>(data));
            }
        }},
        {"CHOPPINESS", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"CLOUD", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeCloudStruct(bytes);
                data = new DevStudio::CloudStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeCloudStruct(*static_cast<DevStudio::CloudStruct*>(data));
            }
        }},
        {"CODE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodechar(bytes);
                data = new char(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodechar(*static_cast<char*>(data));
            }
        }},
        {"CODE_NAME", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeDesignatorCodeNameEnum(bytes);
                data = new DevStudio::DesignatorCodeNameEnum::DesignatorCodeNameEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeDesignatorCodeNameEnum(*static_cast<DevStudio::DesignatorCodeNameEnum::DesignatorCodeNameEnum*>(data));
            }
        }},
        {"COMBAT_VALUE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"COMMENT", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodewstring(bytes);
                data = new std::wstring(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodewstring(*static_cast<std::wstring*>(data));
            }
        }},
        {"COMPLIANCE_STATE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeComplianceStateEnum(bytes);
                data = new DevStudio::ComplianceStateEnum::ComplianceStateEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeComplianceStateEnum(*static_cast<DevStudio::ComplianceStateEnum::ComplianceStateEnum*>(data));
            }
        }},
        {"CONSTANT_GRID", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeEnvironmentGridTypeEnum(bytes);
                data = new DevStudio::EnvironmentGridTypeEnum::EnvironmentGridTypeEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeEnvironmentGridTypeEnum(*static_cast<DevStudio::EnvironmentGridTypeEnum::EnvironmentGridTypeEnum*>(data));
            }
        }},
        {"COORDINATE_SYSTEM", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeEnvironmentDataCoordinateSystemEnum(bytes);
                data = new DevStudio::EnvironmentDataCoordinateSystemEnum::EnvironmentDataCoordinateSystemEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeEnvironmentDataCoordinateSystemEnum(*static_cast<DevStudio::EnvironmentDataCoordinateSystemEnum::EnvironmentDataCoordinateSystemEnum*>(data));
            }
        }},
        {"COUNTRY_CODE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeint(bytes);
                data = new int(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeint(*static_cast<int*>(data));
            }
        }},
        {"CRYPTOGRAPHIC_MODE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeCryptographicModeEnum(bytes);
                data = new DevStudio::CryptographicModeEnum::CryptographicModeEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeCryptographicModeEnum(*static_cast<DevStudio::CryptographicModeEnum::CryptographicModeEnum*>(data));
            }
        }},
        {"CRYPTO_SYSTEM", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeCryptographicSystemTypeEnum(bytes);
                data = new DevStudio::CryptographicSystemTypeEnum::CryptographicSystemTypeEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeCryptographicSystemTypeEnum(*static_cast<DevStudio::CryptographicSystemTypeEnum::CryptographicSystemTypeEnum*>(data));
            }
        }},
        {"CURRENT", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeCurrentStruct(bytes);
                data = new DevStudio::CurrentStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeCurrentStruct(*static_cast<DevStudio::CurrentStruct*>(data));
            }
        }},
        {"DAMAGED_APPEARANCE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeDamageStatusEnum(bytes);
                data = new DevStudio::DamageStatusEnum::DamageStatusEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeDamageStatusEnum(*static_cast<DevStudio::DamageStatusEnum::DamageStatusEnum*>(data));
            }
        }},
        {"DAMAGE_STATE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeDamageStatusEnum(bytes);
                data = new DevStudio::DamageStatusEnum::DamageStatusEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeDamageStatusEnum(*static_cast<DevStudio::DamageStatusEnum::DamageStatusEnum*>(data));
            }
        }},
        {"DEACTIVATED", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"DEAD_RECKONING_ALGORITHM", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeDeadReckoningAlgorithmEnum(bytes);
                data = new DevStudio::DeadReckoningAlgorithmEnum::DeadReckoningAlgorithmEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeDeadReckoningAlgorithmEnum(*static_cast<DevStudio::DeadReckoningAlgorithmEnum::DeadReckoningAlgorithmEnum*>(data));
            }
        }},
        {"DECONTAMINATION_PERIOD", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeint(bytes);
                data = new int(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeint(*static_cast<int*>(data));
            }
        }},
        {"DESCRIPTION", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodePhysicalNetworkDescriptionVariant(bytes);
                data = new DevStudio::PhysicalNetworkDescriptionVariant(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodePhysicalNetworkDescriptionVariant(*static_cast<DevStudio::PhysicalNetworkDescriptionVariant*>(data));
            }
        }},
        {"DESIGNATED_OBJECT_IDENTIFIER", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodestring(bytes);
                data = new std::string(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodestring(*static_cast<std::string*>(data));
            }
        }},
        {"DESIGNATOR_CODE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeDesignatorCodeEnum(bytes);
                data = new DevStudio::DesignatorCodeEnum::DesignatorCodeEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeDesignatorCodeEnum(*static_cast<DevStudio::DesignatorCodeEnum::DesignatorCodeEnum*>(data));
            }
        }},
        {"DESIGNATOR_EMISSION_WAVELENGTH", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"DESIGNATOR_OUTPUT_POWER", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"DESIGNATOR_SPOT_LOCATION", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeWorldLocationStruct(bytes);
                data = new DevStudio::WorldLocationStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeWorldLocationStruct(*static_cast<DevStudio::WorldLocationStruct*>(data));
            }
        }},
        {"DIMENSIONS", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeDimensionStruct(bytes);
                data = new DevStudio::DimensionStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeDimensionStruct(*static_cast<DevStudio::DimensionStruct*>(data));
            }
        }},
        {"DIMENSION_TO_BOW", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"DIMENSION_TO_PORT", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"DIMENSION_TO_STARBOARD", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"DIMENSION_TO_STERN", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"DISTRESS_BEACON_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"DRAUGHT", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"DURATION", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeint(bytes);
                data = new int(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeint(*static_cast<int*>(data));
            }
        }},
        {"ECHELON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeEchelonEnum(bytes);
                data = new DevStudio::EchelonEnum::EchelonEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeEchelonEnum(*static_cast<DevStudio::EchelonEnum::EchelonEnum*>(data));
            }
        }},
        {"EFFECT", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"EFFECTIVE_RADIATED_POWER", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"ELEVATION_BEAMWIDTH", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"ELEVATION_CENTER", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"EMERGENCY_LIGHTS_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"EMERGENCY_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"EMISSION_FREQUENCY", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"EMITTER_FUNCTION_CODE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeEmitterFunctionEnum(bytes);
                data = new DevStudio::EmitterFunctionEnum::EmitterFunctionEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeEmitterFunctionEnum(*static_cast<DevStudio::EmitterFunctionEnum::EmitterFunctionEnum*>(data));
            }
        }},
        {"EMITTER_INDEX", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodechar(bytes);
                data = new char(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodechar(*static_cast<char*>(data));
            }
        }},
        {"EMITTER_SYSTEM_IDENTIFIER", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodestring(bytes);
                data = new std::string(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodestring(*static_cast<std::string*>(data));
            }
        }},
        {"EMITTER_TYPE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeEmitterTypeEnum(bytes);
                data = new DevStudio::EmitterTypeEnum::EmitterTypeEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeEmitterTypeEnum(*static_cast<DevStudio::EmitterTypeEnum::EmitterTypeEnum*>(data));
            }
        }},
        {"ENGINE_SMOKE_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"ENTITY_IDENTIFIER", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeEntityIdentifierStruct(bytes);
                data = new DevStudio::EntityIdentifierStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeEntityIdentifierStruct(*static_cast<DevStudio::EntityIdentifierStruct*>(data));
            }
        }},
        {"ENTITY_TYPE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeEntityTypeStruct(bytes);
                data = new DevStudio::EntityTypeStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeEntityTypeStruct(*static_cast<DevStudio::EntityTypeStruct*>(data));
            }
        }},
        {"ENVIRONMENT_PROCESS_ACTIVE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"ENVIRONMENT_TYPE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeEnvironmentTypeStruct(bytes);
                data = new DevStudio::EnvironmentTypeStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeEnvironmentTypeStruct(*static_cast<DevStudio::EnvironmentTypeStruct*>(data));
            }
        }},
        {"EVENT_IDENTIFIER", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeEventIdentifierStruct(bytes);
                data = new DevStudio::EventIdentifierStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeEventIdentifierStruct(*static_cast<DevStudio::EventIdentifierStruct*>(data));
            }
        }},
        {"EXPOSURE_TYPE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeExposureTypeEnum(bytes);
                data = new DevStudio::ExposureTypeEnum::ExposureTypeEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeExposureTypeEnum(*static_cast<DevStudio::ExposureTypeEnum::ExposureTypeEnum*>(data));
            }
        }},
        {"EXTERNAL_LIGHTS_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"FEDERATE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodewstring(bytes);
                data = new std::wstring(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodewstring(*static_cast<std::wstring*>(data));
            }
        }},
        {"FIRE_POWER_DISABLED", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"FISHING_WITH_LINES_LIGHTS_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"FISHING_WITH_NETS_LIGHTS_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"FLAMES_PRESENT", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"FLAMING", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"FLASH_LIGHTS_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"FLIGHT_DECK_LIGHTS", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodechar(bytes);
                data = new char(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodechar(*static_cast<char*>(data));
            }
        }},
        {"FORCE_IDENTIFIER", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeForceIdentifierEnum(bytes);
                data = new DevStudio::ForceIdentifierEnum::ForceIdentifierEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeForceIdentifierEnum(*static_cast<DevStudio::ForceIdentifierEnum::ForceIdentifierEnum*>(data));
            }
        }},
        {"FORMATION", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeFormationEnum(bytes);
                data = new DevStudio::FormationEnum::FormationEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeFormationEnum(*static_cast<DevStudio::FormationEnum::FormationEnum*>(data));
            }
        }},
        {"FORMATION_LIGHTS_LUMINANCE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodechar(bytes);
                data = new char(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodechar(*static_cast<char*>(data));
            }
        }},
        {"FORMATION_LIGHTS_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"FREQUENCY_BANDWIDTH", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"FREQUENCY_RANGE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"FRONT_VELOCITY", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"FRONT_WIDTH", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"FUNCTION_CODE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeActiveSonarFunctionCodeEnum(bytes);
                data = new DevStudio::ActiveSonarFunctionCodeEnum::ActiveSonarFunctionCodeEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeActiveSonarFunctionCodeEnum(*static_cast<DevStudio::ActiveSonarFunctionCodeEnum::ActiveSonarFunctionCodeEnum*>(data));
            }
        }},
        {"GEO_REFERENCE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeGeoReferenceVariant(bytes);
                data = new DevStudio::GeoReferenceVariant(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeGeoReferenceVariant(*static_cast<DevStudio::GeoReferenceVariant*>(data));
            }
        }},
        {"GLIDE_PATH_INDICATOR_BEAM", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodechar(bytes);
                data = new char(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodechar(*static_cast<char*>(data));
            }
        }},
        {"GRID_IDENTIFIER", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeEntityIdentifierStruct(bytes);
                data = new DevStudio::EntityIdentifierStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeEntityIdentifierStruct(*static_cast<DevStudio::EntityIdentifierStruct*>(data));
            }
        }},
        {"HAS_AMMUNITION_SUPPLY_CAP", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"HAS_FUEL_SUPPLY_CAP", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"HAS_RECOVERY_CAP", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"HAS_REPAIR_CAP", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"HATCH_STATE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeHatchStateEnum(bytes);
                data = new DevStudio::HatchStateEnum::HatchStateEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeHatchStateEnum(*static_cast<DevStudio::HatchStateEnum::HatchStateEnum*>(data));
            }
        }},
        {"HAZARD_TYPE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeHazardTypeEnum2(bytes);
                data = new DevStudio::HazardTypeEnum2::HazardTypeEnum2(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeHazardTypeEnum2(*static_cast<DevStudio::HazardTypeEnum2::HazardTypeEnum2*>(data));
            }
        }},
        {"HAZE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeHazeStruct(bytes);
                data = new DevStudio::HazeStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeHazeStruct(*static_cast<DevStudio::HazeStruct*>(data));
            }
        }},
        {"HEAD_LIGHTS_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"HEIGHT", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"HIGH_DENSITY_JAM", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"HIGH_DENSITY_TRACK", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"HORIZON_BAR_LIGHTS", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodechar(bytes);
                data = new char(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodechar(*static_cast<char*>(data));
            }
        }},
        {"HOST_OBJECT_IDENTIFIER", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodestring(bytes);
                data = new std::string(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodestring(*static_cast<std::string*>(data));
            }
        }},
        {"HULL_MASKER_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"HUMIDITY", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"ICE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeIceStruct(bytes);
                data = new DevStudio::IceStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeIceStruct(*static_cast<DevStudio::IceStruct*>(data));
            }
        }},
        {"ICE_CONDITION", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeRoadIceConditionEnum(bytes);
                data = new DevStudio::RoadIceConditionEnum::RoadIceConditionEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeRoadIceConditionEnum(*static_cast<DevStudio::RoadIceConditionEnum::RoadIceConditionEnum*>(data));
            }
        }},
        {"IDENT_SQUAWK_FLASH_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"IMMOBILIZED", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"INDICATED_AIRSPEED", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"INFRARED_SIGNATURE_INDEX", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeshort(bytes);
                data = new short(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeshort(*static_cast<short*>(data));
            }
        }},
        {"INTERIOR_LIGHTS_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"INTERNAL_HEAT_SOURCE_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"INTERNAL_LIGHTS_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"IS_CONCEALED", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"IS_DAMAGED", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"IS_HQ", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"IS_MALFUNCTIONING", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"IS_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"IS_PART_OF", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeIsPartOfStruct(bytes);
                data = new DevStudio::IsPartOfStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeIsPartOfStruct(*static_cast<DevStudio::IsPartOfStruct*>(data));
            }
        }},
        {"IS_SILENT", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"LANDING_LIGHTS_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"LANDSLIDE_TYPE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeLandslideTypeEnum(bytes);
                data = new DevStudio::LandslideTypeEnum::LandslideTypeEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeLandslideTypeEnum(*static_cast<DevStudio::LandslideTypeEnum::LandslideTypeEnum*>(data));
            }
        }},
        {"LANE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeMinefieldLaneEnum(bytes);
                data = new DevStudio::MinefieldLaneEnum::MinefieldLaneEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeMinefieldLaneEnum(*static_cast<DevStudio::MinefieldLaneEnum::MinefieldLaneEnum*>(data));
            }
        }},
        {"LAUNCHER_FLASH_PRESENT", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"LAUNCHER_RAISED", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"LAYER", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeLayerStruct(bytes);
                data = new DevStudio::LayerStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeLayerStruct(*static_cast<DevStudio::LayerStruct*>(data));
            }
        }},
        {"LEFT_INDICATOR_LIGHTS_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"LEVEL", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"LEVEL_CHANGE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"LIGHTNING_INTENSITY", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"LIGHTNING_TYPE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeLightningTypeEnum(bytes);
                data = new DevStudio::LightningTypeEnum::LightningTypeEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeLightningTypeEnum(*static_cast<DevStudio::LightningTypeEnum::LightningTypeEnum*>(data));
            }
        }},
        {"LIGHTS_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"LOCATION", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeWorldLocationStruct(bytes);
                data = new DevStudio::WorldLocationStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeWorldLocationStruct(*static_cast<DevStudio::WorldLocationStruct*>(data));
            }
        }},
        {"MACH_NUMBER", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"MAGNITUDE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"MARKING", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeMarkingStruct(bytes);
                data = new DevStudio::MarkingStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeMarkingStruct(*static_cast<DevStudio::MarkingStruct*>(data));
            }
        }},
        {"MINEFIELD_APPEARANCE_TYPE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeMinefieldTypeEnum(bytes);
                data = new DevStudio::MinefieldTypeEnum::MinefieldTypeEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeMinefieldTypeEnum(*static_cast<DevStudio::MinefieldTypeEnum::MinefieldTypeEnum*>(data));
            }
        }},
        {"MINEFIELD_IDENTIFIER", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodestring(bytes);
                data = new std::string(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodestring(*static_cast<std::string*>(data));
            }
        }},
        {"MINEFIELD_LOCATION", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeWorldLocationStruct(bytes);
                data = new DevStudio::WorldLocationStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeWorldLocationStruct(*static_cast<DevStudio::WorldLocationStruct*>(data));
            }
        }},
        {"MINEFIELD_ORIENTATION", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeOrientationStruct(bytes);
                data = new DevStudio::OrientationStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeOrientationStruct(*static_cast<DevStudio::OrientationStruct*>(data));
            }
        }},
        {"MINEFIELD_TYPE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeEntityTypeStruct(bytes);
                data = new DevStudio::EntityTypeStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeEntityTypeStruct(*static_cast<DevStudio::EntityTypeStruct*>(data));
            }
        }},
        {"MINE_HUNTING_LIGHTS_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"MINE_TYPE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeEntityTypeStruct(bytes);
                data = new DevStudio::EntityTypeStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeEntityTypeStruct(*static_cast<DevStudio::EntityTypeStruct*>(data));
            }
        }},
        {"MISSION_KILL", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"MODEL_TYPE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeEnvironmentModelTypeEnum(bytes);
                data = new DevStudio::EnvironmentModelTypeEnum::EnvironmentModelTypeEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeEnvironmentModelTypeEnum(*static_cast<DevStudio::EnvironmentModelTypeEnum::EnvironmentModelTypeEnum*>(data));
            }
        }},
        {"MOISTURE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeSurfaceMoistureEnum(bytes);
                data = new DevStudio::SurfaceMoistureEnum::SurfaceMoistureEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeSurfaceMoistureEnum(*static_cast<DevStudio::SurfaceMoistureEnum::SurfaceMoistureEnum*>(data));
            }
        }},
        {"NAME", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodewstring(bytes);
                data = new std::wstring(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodewstring(*static_cast<std::wstring*>(data));
            }
        }},
        {"NAVIGATION_LIGHTS_FLASHING", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeNavigationLightFlashingEnum(bytes);
                data = new DevStudio::NavigationLightFlashingEnum::NavigationLightFlashingEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeNavigationLightFlashingEnum(*static_cast<DevStudio::NavigationLightFlashingEnum::NavigationLightFlashingEnum*>(data));
            }
        }},
        {"NAVIGATION_LIGHTS_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"NETWORK_TYPE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeCommunicationNetworkTypeEnum(bytes);
                data = new DevStudio::CommunicationNetworkTypeEnum::CommunicationNetworkTypeEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeCommunicationNetworkTypeEnum(*static_cast<DevStudio::CommunicationNetworkTypeEnum::CommunicationNetworkTypeEnum*>(data));
            }
        }},
        {"NOT_UNDER_COMMAND_LIGHTS_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"NUMBER_OF_GRID_AXES", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodechar(bytes);
                data = new char(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodechar(*static_cast<char*>(data));
            }
        }},
        {"NUMBER_OF_SILENT_ENTITIES", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeshort(bytes);
                data = new short(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeshort(*static_cast<short*>(data));
            }
        }},
        {"OBJECT_IDENTIFIER", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeEntityIdentifierStruct(bytes);
                data = new DevStudio::EntityIdentifierStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeEntityIdentifierStruct(*static_cast<DevStudio::EntityIdentifierStruct*>(data));
            }
        }},
        {"OBJECT_PRE_DISTRIBUTED", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"OBJECT_TYPE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeEnvironmentObjectTypeStruct(bytes);
                data = new DevStudio::EnvironmentObjectTypeStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeEnvironmentObjectTypeStruct(*static_cast<DevStudio::EnvironmentObjectTypeStruct*>(data));
            }
        }},
        {"OPACITY_CODE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeOpacityCodeEnum(bytes);
                data = new DevStudio::OpacityCodeEnum::OpacityCodeEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeOpacityCodeEnum(*static_cast<DevStudio::OpacityCodeEnum::OpacityCodeEnum*>(data));
            }
        }},
        {"ORIENTATION", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeOrientationStruct(bytes);
                data = new DevStudio::OrientationStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeOrientationStruct(*static_cast<DevStudio::OrientationStruct*>(data));
            }
        }},
        {"POWER_PLANT_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"POWER_REDUCTION", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"PRECIPITATION", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodePrecipitationStruct(bytes);
                data = new DevStudio::PrecipitationStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodePrecipitationStruct(*static_cast<DevStudio::PrecipitationStruct*>(data));
            }
        }},
        {"PRIMARY_WEAPON_STATE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeWeaponStateEnum(bytes);
                data = new DevStudio::WeaponStateEnum::WeaponStateEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeWeaponStateEnum(*static_cast<DevStudio::WeaponStateEnum::WeaponStateEnum*>(data));
            }
        }},
        {"PROCESSING_TIME", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeint(bytes);
                data = new int(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeint(*static_cast<int*>(data));
            }
        }},
        {"PROCESS_IDENTIFIER", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeEntityIdentifierStruct(bytes);
                data = new DevStudio::EntityIdentifierStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeEntityIdentifierStruct(*static_cast<DevStudio::EntityIdentifierStruct*>(data));
            }
        }},
        {"PROPULSION_PLANT_CONFIGURATION", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodePropulsionPlantEnum(bytes);
                data = new DevStudio::PropulsionPlantEnum::PropulsionPlantEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodePropulsionPlantEnum(*static_cast<DevStudio::PropulsionPlantEnum::PropulsionPlantEnum*>(data));
            }
        }},
        {"PROTECTION_LEVEL", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"PROTOCOL_MODE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeMinefieldProtocolEnum(bytes);
                data = new DevStudio::MinefieldProtocolEnum::MinefieldProtocolEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeMinefieldProtocolEnum(*static_cast<DevStudio::MinefieldProtocolEnum::MinefieldProtocolEnum*>(data));
            }
        }},
        {"PULSE_REPETITION_FREQUENCY", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"PULSE_WIDTH", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"RADAR_CROSS_SECTION_SIGNATURE_INDEX", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeshort(bytes);
                data = new short(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeshort(*static_cast<short*>(data));
            }
        }},
        {"RADAR_ENHANCEMENT", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"RADIO_INPUT_SOURCE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeRadioInputSourceEnum(bytes);
                data = new DevStudio::RadioInputSourceEnum::RadioInputSourceEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeRadioInputSourceEnum(*static_cast<DevStudio::RadioInputSourceEnum::RadioInputSourceEnum*>(data));
            }
        }},
        {"RADIO_SYSTEM_TYPE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeEntityTypeStruct(bytes);
                data = new DevStudio::EntityTypeStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeEntityTypeStruct(*static_cast<DevStudio::EntityTypeStruct*>(data));
            }
        }},
        {"RADIUS", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodedouble(bytes);
                data = new double(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodedouble(*static_cast<double*>(data));
            }
        }},
        {"RAIN_ACCUMULATION", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"RAMP_DEPLOYED", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"RECEIVED_POWER", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"RECEIVED_TRANSMITTER_IDENTIFIER", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodestring(bytes);
                data = new std::string(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodestring(*static_cast<std::string*>(data));
            }
        }},
        {"RECEIVER_OPERATIONAL_STATUS", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeReceiverOperationalStatusEnum(bytes);
                data = new DevStudio::ReceiverOperationalStatusEnum::ReceiverOperationalStatusEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeReceiverOperationalStatusEnum(*static_cast<DevStudio::ReceiverOperationalStatusEnum::ReceiverOperationalStatusEnum*>(data));
            }
        }},
        {"REFERENCED_OBJECT_IDENTIFIER", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodestring(bytes);
                data = new std::string(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodestring(*static_cast<std::string*>(data));
            }
        }},
        {"REFERENCE_ALTITUDE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodedouble(bytes);
                data = new double(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodedouble(*static_cast<double*>(data));
            }
        }},
        {"RELATIVE_POSITION", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeRelativePositionStruct(bytes);
                data = new DevStudio::RelativePositionStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeRelativePositionStruct(*static_cast<DevStudio::RelativePositionStruct*>(data));
            }
        }},
        {"RELATIVE_SPATIAL", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeSpatialVariantStruct(bytes);
                data = new DevStudio::SpatialVariantStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeSpatialVariantStruct(*static_cast<DevStudio::SpatialVariantStruct*>(data));
            }
        }},
        {"RELATIVE_SPOT_LOCATION", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeRelativePositionStruct(bytes);
                data = new DevStudio::RelativePositionStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeRelativePositionStruct(*static_cast<DevStudio::RelativePositionStruct*>(data));
            }
        }},
        {"RIGHT_INDICATOR_LIGHTS_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"RUNNING_LIGHTS_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"SALINITY", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"SCAN_PATTERN", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeActiveSonarScanPatternEnum(bytes);
                data = new DevStudio::ActiveSonarScanPatternEnum::ActiveSonarScanPatternEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeActiveSonarScanPatternEnum(*static_cast<DevStudio::ActiveSonarScanPatternEnum::ActiveSonarScanPatternEnum*>(data));
            }
        }},
        {"SEA_STATE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeSeaStateEnum(bytes);
                data = new DevStudio::SeaStateEnum::SeaStateEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeSeaStateEnum(*static_cast<DevStudio::SeaStateEnum::SeaStateEnum*>(data));
            }
        }},
        {"SECONDARY_OPERATIONAL_DATA_PARAMETER1", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeIffOperationalParameter1Enum(bytes);
                data = new DevStudio::IffOperationalParameter1Enum::IffOperationalParameter1Enum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeIffOperationalParameter1Enum(*static_cast<DevStudio::IffOperationalParameter1Enum::IffOperationalParameter1Enum*>(data));
            }
        }},
        {"SECONDARY_OPERATIONAL_DATA_PARAMETER2", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeIffOperationalParameter2Enum(bytes);
                data = new DevStudio::IffOperationalParameter2Enum::IffOperationalParameter2Enum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeIffOperationalParameter2Enum(*static_cast<DevStudio::IffOperationalParameter2Enum::IffOperationalParameter2Enum*>(data));
            }
        }},
        {"SECONDARY_WEAPON_STATE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeWeaponStateEnum(bytes);
                data = new DevStudio::WeaponStateEnum::WeaponStateEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeWeaponStateEnum(*static_cast<DevStudio::WeaponStateEnum::WeaponStateEnum*>(data));
            }
        }},
        {"SERVICE_TYPE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeCommunicationServiceTypeEnum(bytes);
                data = new DevStudio::CommunicationServiceTypeEnum::CommunicationServiceTypeEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeCommunicationServiceTypeEnum(*static_cast<DevStudio::CommunicationServiceTypeEnum::CommunicationServiceTypeEnum*>(data));
            }
        }},
        {"SHIP_TYPE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeShipTypeEnum(bytes);
                data = new DevStudio::ShipTypeEnum::ShipTypeEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeShipTypeEnum(*static_cast<DevStudio::ShipTypeEnum::ShipTypeEnum*>(data));
            }
        }},
        {"SMOKE_PLUME_PRESENT", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"SMOKING", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"SNOW", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeSnowStruct(bytes);
                data = new DevStudio::SnowStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeSnowStruct(*static_cast<DevStudio::SnowStruct*>(data));
            }
        }},
        {"SPATIAL", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeSpatialVariantStruct(bytes);
                data = new DevStudio::SpatialVariantStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeSpatialVariantStruct(*static_cast<DevStudio::SpatialVariantStruct*>(data));
            }
        }},
        {"SPOT_LIGHTS_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"SPOT_LINEAR_ACCELERATION_VECTOR", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeAccelerationVectorStruct(bytes);
                data = new DevStudio::AccelerationVectorStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeAccelerationVectorStruct(*static_cast<DevStudio::AccelerationVectorStruct*>(data));
            }
        }},
        {"SPREAD_SPECTRUM", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeSpreadSpectrumVariantStruct(bytes);
                data = new DevStudio::SpreadSpectrumVariantStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeSpreadSpectrumVariantStruct(*static_cast<DevStudio::SpreadSpectrumVariantStruct*>(data));
            }
        }},
        {"STANCE_CODE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeStanceCodeEnum(bytes);
                data = new DevStudio::StanceCodeEnum::StanceCodeEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeStanceCodeEnum(*static_cast<DevStudio::StanceCodeEnum::StanceCodeEnum*>(data));
            }
        }},
        {"STATE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"STATUS", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeActiveStatusEnum(bytes);
                data = new DevStudio::ActiveStatusEnum::ActiveStatusEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeActiveStatusEnum(*static_cast<DevStudio::ActiveStatusEnum::ActiveStatusEnum*>(data));
            }
        }},
        {"STI_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"SURFACE_ALTITUDE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"SURGE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"SWEEP_SYNCH", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"SWELL", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeWaveStruct(bytes);
                data = new DevStudio::WaveStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeWaveStruct(*static_cast<DevStudio::WaveStruct*>(data));
            }
        }},
        {"SYMBOL", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeSymbolStruct(bytes);
                data = new DevStudio::SymbolStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeSymbolStruct(*static_cast<DevStudio::SymbolStruct*>(data));
            }
        }},
        {"SYSTEM_IS_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"SYSTEM_IS_OPERATIONAL", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"SYSTEM_MODE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeIffSystemModeEnum(bytes);
                data = new DevStudio::IffSystemModeEnum::IffSystemModeEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeIffSystemModeEnum(*static_cast<DevStudio::IffSystemModeEnum::IffSystemModeEnum*>(data));
            }
        }},
        {"SYSTEM_NAME", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeIffSystemNameEnum(bytes);
                data = new DevStudio::IffSystemNameEnum::IffSystemNameEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeIffSystemNameEnum(*static_cast<DevStudio::IffSystemNameEnum::IffSystemNameEnum*>(data));
            }
        }},
        {"SYSTEM_TYPE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeIffSystemTypeEnum(bytes);
                data = new DevStudio::IffSystemTypeEnum::IffSystemTypeEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeIffSystemTypeEnum(*static_cast<DevStudio::IffSystemTypeEnum::IffSystemTypeEnum*>(data));
            }
        }},
        {"TAIL_LIGHTS_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"TASK_DEFINITION", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeTaskDefinition(bytes);
                data = new DevStudio::TaskDefinition(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeTaskDefinition(*static_cast<DevStudio::TaskDefinition*>(data));
            }
        }},
        {"TASK_PROGRESS", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeTaskProgress(bytes);
                data = new DevStudio::TaskProgress(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeTaskProgress(*static_cast<DevStudio::TaskProgress*>(data));
            }
        }},
        {"TAXIWAY_LIGHTS_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"TEMPERATURE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"TENT_DEPLOYED", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"THICKNESS", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"TIME_HOP_IN_USE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"TRAILING_EFFECTS_CODE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeTrailingEffectsCodeEnum(bytes);
                data = new DevStudio::TrailingEffectsCodeEnum::TrailingEffectsCodeEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeTrailingEffectsCodeEnum(*static_cast<DevStudio::TrailingEffectsCodeEnum::TrailingEffectsCodeEnum*>(data));
            }
        }},
        {"TRANSMITTED_POWER", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"TRANSMITTER_OPERATIONAL_STATUS", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeTransmitterOperationalStatusEnum(bytes);
                data = new DevStudio::TransmitterOperationalStatusEnum::TransmitterOperationalStatusEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeTransmitterOperationalStatusEnum(*static_cast<DevStudio::TransmitterOperationalStatusEnum::TransmitterOperationalStatusEnum*>(data));
            }
        }},
        {"TRANSMITTER_STATUS", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeTransmitterOperationalStatusEnum(bytes);
                data = new DevStudio::TransmitterOperationalStatusEnum::TransmitterOperationalStatusEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeTransmitterOperationalStatusEnum(*static_cast<DevStudio::TransmitterOperationalStatusEnum::TransmitterOperationalStatusEnum*>(data));
            }
        }},
        {"TRIAGE_LEVEL", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeCBRNDamageEnum(bytes);
                data = new DevStudio::CBRNDamageEnum::CBRNDamageEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeCBRNDamageEnum(*static_cast<DevStudio::CBRNDamageEnum::CBRNDamageEnum*>(data));
            }
        }},
        {"TRUE_AIRSPEED", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"TYPE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeEnvironmentTypeStruct(bytes);
                data = new DevStudio::EnvironmentTypeStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeEnvironmentTypeStruct(*static_cast<DevStudio::EnvironmentTypeStruct*>(data));
            }
        }},
        {"UPDATE_FREQUENCY", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeint(bytes);
                data = new int(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeint(*static_cast<int*>(data));
            }
        }},
        {"UPPER_DECK_FLOOD_LIGHTS_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"UPPER_DECK_LIGHTS_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"VECTOR_DIMENSION", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodechar(bytes);
                data = new char(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodechar(*static_cast<char*>(data));
            }
        }},
        {"VISIBILITY_RANGE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"WARNING_LIGHTS_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"WAVE", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeWaveStruct(bytes);
                data = new DevStudio::WaveStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeWaveStruct(*static_cast<DevStudio::WaveStruct*>(data));
            }
        }},
        {"WAVE_OFF_LIGHT_ON", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"WEAPONS_CONTROL_ORDER", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeWeaponControlOrderEnum(bytes);
                data = new DevStudio::WeaponControlOrderEnum::WeaponControlOrderEnum(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeWeaponControlOrderEnum(*static_cast<DevStudio::WeaponControlOrderEnum::WeaponControlOrderEnum*>(data));
            }
        }},
        {"WEIGHT_ON_WHEELS", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodebool(bytes);
                data = new bool(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodebool(*static_cast<bool*>(data));
            }
        }},
        {"WIDTH", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"WIND", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeWindStruct(bytes);
                data = new DevStudio::WindStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeWindStruct(*static_cast<DevStudio::WindStruct*>(data));
            }
        }},
        {"WIND_SPEED", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodefloat(bytes);
                data = new float(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodefloat(*static_cast<float*>(data));
            }
        }},
        {"WORLD_LOCATION", {
            [](void*& data, const std::vector<char>& bytes) {
                auto decoded = Decoders::getInstance().decodeWorldLocationStruct(bytes);
                data = new DevStudio::WorldLocationStruct(decoded);
            },
            [](void* data) {
                return Encoders::getInstance().encodeWorldLocationStruct(*static_cast<DevStudio::WorldLocationStruct*>(data));
            }
        }},
// === AUTO-GENERATED END ===
//pragma endregion
		};

		void AttributeValuePair::decode(const AttributeValuePairStruct& pairStruct)
		{
			auto it = attributeMetaTable.find(attributeName);
			if (it != attributeMetaTable.end())
				it->second.decodeFunc(data, pairStruct.numberOfBytesAValue);
		}

		std::vector<char> AttributeValuePair::encode()
		{
			auto it = attributeMetaTable.find(attributeName);
            if (it != attributeMetaTable.end())
            {
     //           if(!data)
					//throw std::runtime_error("Null pointer received in encoder lambda for attribute: " + attributeName);

				return it->second.encodeFunc(data);
            }
		}

		AttributeValuePairStruct AttributeValuePair::getAttributeValuePairStruct()
		{
			std::cout << "AttributeValuePair::getAttributeValuePairStruct() called for attribute: " << attributeName << std::endl;
			AttributeValuePairStruct ret;
			ret.attributeHandle = attribute;
			ret.numberOfBytesAValue = encode();
			ret.paddingTo32.reserve(4 - (ret.numberOfBytesAValue.size() % 4));
			for (size_t i = 0; i < ret.paddingTo32.size(); ++i)
				ret.paddingTo32.push_back(0);

			std::cout << std::hex << std::setfill('0');
			for (size_t i = 0; i < ret.numberOfBytesAValue.size(); ++i) {
				std::cout << std::setw(2) << (static_cast<unsigned int>(static_cast<unsigned char>(ret.numberOfBytesAValue[i])));
				if (i != ret.numberOfBytesAValue.size() - 1)
					std::cout << " ";
			}
			std::cout << std::dec << std::endl;

			return ret;
		}
	}
}

